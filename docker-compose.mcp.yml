# Deprecated - All MCP servers are now in docker-compose.yml
# Use: docker compose up -d

# This file is kept for reference only.
# All services (github-mcp, postgres-mcp, gitlab-ufal-mcp, web-search, code-interpreter-mcp)
# are now defined directly in docker-compose.yml.